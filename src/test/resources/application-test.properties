# Tests build their own in-memory database from the same migrations, without the demo data.
datasources.default.url=jdbc:h2:mem:dogs-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE

# A free port, so a running development server does not clash with a test run.
micronaut.server.port=-1
